# Merge the keys you need into ~/.codex/config.toml.
# Verify model names and reasoning levels against the current Codex model catalog.

model = "gpt-5.6-sol"
model_reasoning_effort = "high"
plan_mode_reasoning_effort = "xhigh"
