# Handoff: {{short-topic}}

| 项 | 值 |
|---|---|
| 日期 | {{YYYY-MM-DD}} |
| 交出角色 | {{设计 / 规划 / 实现}} |
| 接手角色 | {{规划 / 实现 / 审查}} |
| 停点 | {{设计完成 / 工单拆完 / 实现到一半}} |
| 状态 | `[ready]` |

## 目标与非目标

**目标**：{{这一棒最终要做成什么，一两句}}

**非目标**：
- {{明确不做的事}}

## 当前事实

已在其他文档中定义的只链接，不复述。

- {{事实一}} —— 依据：`{{path/to/file}}`
- {{事实二}} —— 依据：`{{command}}` 输出 {{结果}}
- 计划：`plans/{{plan-dir}}/00-overview.md`（如有，链接到既有计划，不复述）

## 已完成

每项附可核实证据；没有证据的不写在这里。

- [x] {{已完成项}} —— `{{path}}`；验证：`{{command}}` → {{结果}}

## 下一步

第一条必须是接手者启动后立刻能做的动作。

1. {{立刻可做的动作}}
2. {{后续动作}}

## 验证方式

```bash
{{command}}
```

预期：{{结果}}

## 开放问题

**接手者可自决**：
- {{问题}}

**需要用户决定**：
- {{问题}}

## 接手指令

- 先读：`{{path}}`、`{{path}}`
- 不要动：`{{path}}`（原因：{{一句话}}）
- 待清理的临时改动：{{无 / 逐条列出}}
- 完成后：运行上面的验证方式，把本文件状态改为 `[done]`
